# ============================================================================
# Agent Demo 环境变量配置示例
# ============================================================================
# 复制此文件为 .env 并填入真实的配置值
# 警告：.env 文件已被 .gitignore 忽略，不会被提交到 Git
# ============================================================================

# ----------------------------------------------------------------------------
# Spring 配置
# ----------------------------------------------------------------------------
# 激活的配置文件：local（本地开发）或 prod（生产环境）
SPRING_PROFILE=local

# 服务端口（默认 8090）
SERVER_PORT=8090

# 日志级别：DEBUG, INFO, WARN, ERROR
LOG_LEVEL=DEBUG

# ----------------------------------------------------------------------------
# 数据库配置
# ----------------------------------------------------------------------------
# 数据库连接地址（本地开发使用 localhost，生产环境使用服务器 IP）
DB_HOST=localhost
DB_PORT=15432
DB_NAME=agent_db
DB_USERNAME=postgres
DB_PASSWORD=postgres

# ----------------------------------------------------------------------------
# Redis 配置
# ----------------------------------------------------------------------------
# Redis 连接地址（本地开发使用 localhost，生产环境使用服务器 IP）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ----------------------------------------------------------------------------
# AI 模型配置
# ----------------------------------------------------------------------------
# 阿里云通义千问 API Key（推荐用于生产环境）
# 获取地址: https://dashscope.console.aliyun.com/apiKey
AI_API_KEY=your_dashscope_api_key_here
AI_MODEL=qwen-max
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=2000

# Ollama 本地模型配置（推荐用于本地开发）
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b
OLLAMA_TEMPERATURE=0.7

# 选择模型提供商：openai（通义千问）或 ollama（本地模型）
AGENT_MODEL_PROVIDER=openai

# ----------------------------------------------------------------------------
# 安全配置
# ----------------------------------------------------------------------------
# 是否启用安全认证（生产环境建议设置为 true）
AGENT_SECURITY_ENABLED=true

# API Key（用于客户端调用 API）
AGENT_API_KEY=your_agent_api_key_here

# ----------------------------------------------------------------------------
# MCP 工具配置
# ----------------------------------------------------------------------------
# 是否启用 MCP 功能
MCP_ENABLED=true

# NPX 命令路径（Windows 本地开发可能需要完整路径，如：C:\Users\...\npx.cmd）
NPX_CMD=npx

# 项目路径（用于文件系统 MCP 工具）
PROJECT_PATH=E:\CODE\JAVA\agent-demo

# 文件系统工具
MCP_FILESYSTEM_ENABLED=true

# GitHub 工具
MCP_GITHUB_ENABLED=false
GITHUB_TOKEN=your_github_token_here

# 数据库工具
MCP_DATABASE_ENABLED=true

# Brave 搜索工具
MCP_BRAVE_ENABLED=false
BRAVE_API_KEY=your_brave_api_key_here

# ----------------------------------------------------------------------------
# 生产环境额外配置
# ----------------------------------------------------------------------------
# 日志文件路径（生产环境）
LOG_PATH=/var/log/agent-demo

